# Python compiled files
__pycache__/
*.py[cod]
*.pyo

# Pytest cache
.pytest_cache/

# Qt compiled resources (regenerated by make)
resources.py

# Claude Code local settings
.claude/

# Editor / OS
*.swp
.DS_Store

# Downloaded binary (fetched by install_pmtiles.sh) + built zip
go-pmtiles_*.tar.gz
mapsplat.zip

# Local pre-refactor backups (kept out of git + zip)
*.orig-*

# A/B dock variant snapshots
*.tabs-*
*.phase1-*
symbology-style.db
.ruff_cache/

# Secrets / credentials (never commit)
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*_rsa
credentials.json
secrets.*
